#Code to run BiSSEbox
##load packages
library(ape)
library(geiger)
library(diversitree)

loop for length of trait nr {
  make.bisse
  starting point
  etc.

  run all the likelihood constraint MODELS
  evaluate the best one
  run MCMC of that one
  save outputs
}



Save output to allow seeing all and plotting all and having a summary of the results
